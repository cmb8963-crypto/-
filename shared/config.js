/*
 * config.js - the only setting the website needs.
 *
 * After deploying the Apps Script backend (see docs/SETUP.md), paste its
 * "Web app URL" below. The URL is not a secret: every request still needs a
 * valid password-based token that only the backend can issue.
 */
window.CRM_CONFIG = {
  apiUrl: ""
};
